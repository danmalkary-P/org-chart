PORT=3000
PUBLIC_BASE_URL=http://localhost:3000

# mock is the safest hackathon default. live requires PYLON_API_TOKEN.
DEMO_MODE=mock

PYLON_API_BASE=https://api.usepylon.com
PYLON_API_TOKEN=

# Reserved for a future LLM-backed generator. The current implementation has
# a deterministic local generator so demos work without network access.
OPENAI_API_KEY=
